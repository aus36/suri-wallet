import { Text, View, StyleSheet } from 'react-native';

const Credentials = () => {
    return (
        <View>
            <Text>Credentials Screen</Text>
        </View>
    );
};

export default Credentials;